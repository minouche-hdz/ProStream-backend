# 🏗️ Architecture ProStream Backend

## 📐 Vue d'Ensemble

```
┌─────────────────────────────────────────────────────────────────┐
│                        ProStream Backend                        │
│                         (Docker Compose)                        │
└─────────────────────────────────────────────────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
         ┌──────────▼──────────┐   ┌─────────▼──────────┐
         │   PostgreSQL DB     │   │   NestJS App       │
         │   (postgres:13)     │   │   (Multi-stage)    │
         │                     │   │                    │
         │  Port: 5432         │   │  Port: 3000        │
         │  User: postgres     │   │  User: nestjs      │
         │  Health: ✓          │   │  Health: ✓         │
         └──────────┬──────────┘   └─────────┬──────────┘
                    │                         │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │  prostream-network      │
                    │  (Bridge Network)       │
                    └─────────────────────────┘
```

---

## 🐳 Architecture Docker Multi-Stage

### Stage 1: Dependencies
```
┌─────────────────────────────────────┐
│  FROM node:24-alpine AS dependencies│
│                                     │
│  📦 COPY package*.json              │
│  📥 RUN npm ci (all deps)           │
│                                     │
│  Output: node_modules/ (full)       │
└─────────────────────────────────────┘
                  │
                  ▼
```

### Stage 2: Build
```
┌─────────────────────────────────────┐
│  FROM node:24-alpine AS build       │
│                                     │
│  📦 COPY node_modules from Stage 1  │
│  📄 COPY source code                │
│  🏗️  RUN npm run build              │
│                                     │
│  Output: dist/                      │
└─────────────────────────────────────┘
                  │
                  ▼
```

### Stage 3: Production ⭐
```
┌─────────────────────────────────────┐
│  FROM node:24-alpine AS production  │
│                                     │
│  🎬 RUN apk add ffmpeg              │
│  👤 RUN adduser nestjs              │
│  📦 RUN npm ci --only=production    │
│  📄 COPY dist/ from Stage 2         │
│  🔒 USER nestjs                     │
│  🏥 HEALTHCHECK                     │
│                                     │
│  Final Image: ~300 MB               │
└─────────────────────────────────────┘
```

**Résultat :**
- ✅ Seulement les dépendances de production
- ✅ Code compilé (JavaScript)
- ✅ Utilisateur non-root
- ✅ Image optimisée

---

## 🌐 Architecture Réseau

```
┌─────────────────────────────────────────────────────────────┐
│                      Host Machine                           │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         Docker Network: prostream-network            │  │
│  │                  (Bridge Mode)                       │  │
│  │                                                      │  │
│  │  ┌─────────────────┐      ┌────────────────────┐   │  │
│  │  │  prostream-db   │      │  prostream-app     │   │  │
│  │  │                 │      │                    │   │  │
│  │  │  IP: 172.x.x.2  │◄────►│  IP: 172.x.x.3     │   │  │
│  │  │  Hostname: db   │      │  Hostname: app     │   │  │
│  │  └────────┬────────┘      └─────────┬──────────┘   │  │
│  │           │                          │              │  │
│  └───────────┼──────────────────────────┼──────────────┘  │
│              │                          │                 │
│         Port 5432                  Port 3000              │
│              │                          │                 │
└──────────────┼──────────────────────────┼─────────────────┘
               │                          │
               ▼                          ▼
         localhost:5432            localhost:3000
```

**Avantages :**
- 🔒 Isolation des services
- 🌐 Communication par hostname (`db`, `app`)
- 🚀 Performance optimale
- 🔧 Facile à scaler

---

## 📊 Flux de Données

### 1. Requête Utilisateur → Streaming

```
┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  User   │────►│   App    │────►│  TMDB    │     │          │
│         │     │          │     │   API    │     │          │
└─────────┘     └────┬─────┘     └──────────┘     │          │
                     │                             │          │
                     ▼                             │          │
                ┌──────────┐                       │          │
                │ Prowlarr │                       │          │
                │   API    │                       │  Docker  │
                └────┬─────┘                       │  Volume  │
                     │                             │          │
                     ▼                             │  hls_    │
                ┌──────────┐                       │  temp/   │
                │AllDebrid │                       │          │
                │   API    │                       │          │
                └────┬─────┘                       │          │
                     │                             │          │
                     ▼                             │          │
                ┌──────────┐     ┌──────────┐     │          │
                │  FFmpeg  │────►│   HLS    │────►│          │
                │Conversion│     │  Files   │     │          │
                └──────────┘     └──────────┘     └──────────┘
                                      │
                                      ▼
                                 ┌─────────┐
                                 │  User   │
                                 │ Stream  │
                                 └─────────┘
```

### 2. Authentification

```
┌─────────┐     ┌──────────┐     ┌──────────┐
│  User   │────►│   App    │────►│    DB    │
│ (Login) │     │  (JWT)   │     │ (Users)  │
└─────────┘     └────┬─────┘     └──────────┘
                     │
                     ▼
                ┌──────────┐
                │   JWT    │
                │  Token   │
                └────┬─────┘
                     │
                     ▼
                ┌─────────┐
                │  User   │
                │(Authed) │
                └─────────┘
```

---

## 🏥 Health Checks

### PostgreSQL Health Check
```
┌─────────────────────────────────────┐
│  Health Check: PostgreSQL           │
│                                     │
│  Command:                           │
│  pg_isready -U prostream_user       │
│             -d prostream_db         │
│                                     │
│  Interval: 10s                      │
│  Timeout:  5s                       │
│  Retries:  5                        │
│  Start:    10s                      │
│                                     │
│  Status: ✓ healthy                  │
└─────────────────────────────────────┘
```

### NestJS App Health Check
```
┌─────────────────────────────────────┐
│  Health Check: NestJS App           │
│                                     │
│  Command:                           │
│  HTTP GET http://localhost:3000/    │
│                                     │
│  Interval: 30s                      │
│  Timeout:  10s                      │
│  Retries:  3                        │
│  Start:    40s                      │
│                                     │
│  Status: ✓ healthy                  │
└─────────────────────────────────────┘
```

**Workflow :**
```
Container Start
      │
      ▼
  Wait 40s (start_period)
      │
      ▼
  Run Health Check
      │
      ├─► Success (200) ──► healthy
      │
      └─► Fail ──► Retry (max 3)
                    │
                    └─► unhealthy
```

---

## 💾 Volumes et Persistance

```
┌─────────────────────────────────────────────────────────┐
│                    Docker Volumes                       │
│                                                         │
│  ┌─────────────────────┐    ┌─────────────────────┐   │
│  │  postgres_data      │    │     hls_data        │   │
│  │                     │    │                     │   │
│  │  /var/lib/          │    │  /app/hls_temp/     │   │
│  │  postgresql/data    │    │                     │   │
│  │                     │    │  - *.m3u8           │   │
│  │  - Database files   │    │  - *.ts             │   │
│  │  - WAL logs         │    │  - *.m4s            │   │
│  │  - Indexes          │    │                     │   │
│  │                     │    │  Auto-cleanup: 24h  │   │
│  │  Persist: ✓         │    │  Persist: ✓         │   │
│  └─────────────────────┘    └─────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 CI/CD Pipeline

```
┌─────────────────────────────────────────────────────────────┐
│                    GitHub Actions                           │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐   ┌───────────────┐   ┌──────────────┐
│  Job: Test    │   │  Job: Build   │   │ Job: Security│
│               │   │               │   │              │
│ • Checkout    │   │ • Checkout    │   │ • Checkout   │
│ • Setup Node  │   │ • Setup Node  │   │ • Trivy Scan │
│ • npm ci      │   │ • npm ci      │   │ • Upload     │
│ • ESLint      │   │ • npm build   │   │   SARIF      │
│ • Tests       │   │ • Upload      │   │              │
│ • Coverage    │   │   Artifacts   │   │ Status: ✓    │
│               │   │               │   │              │
│ Status: ✓     │   │ Status: ✓     │   └──────────────┘
└───────┬───────┘   └───────┬───────┘
        │                   │
        └───────────┬───────┘
                    │
                    ▼
            ┌───────────────┐
            │  Job: Docker  │
            │  (main only)  │
            │               │
            │ • Buildx      │
            │ • Login       │
            │ • Build       │
            │ • Push        │
            │               │
            │ Status: ✓     │
            └───────────────┘
                    │
                    ▼
            ┌───────────────┐
            │ Docker Hub    │
            │ (Registry)    │
            └───────────────┘
```

---

## 📦 Structure des Fichiers

```
ProStream-backend/
│
├── 📄 Dockerfile (Multi-stage)
├── 📄 docker-compose.yml
├── 📄 .dockerignore
├── 📄 .env.example
├── 📄 .gitignore
│
├── 📁 .github/
│   └── workflows/
│       └── ci.yml (CI/CD Pipeline)
│
├── 📁 src/
│   ├── app.module.ts
│   ├── main.ts
│   ├── alldebrid/
│   ├── prowlarr/
│   ├── streaming/
│   ├── tmdb/
│   └── users/
│
├── 📁 dist/ (Generated)
│   └── *.js (Compiled)
│
├── 📁 node_modules/ (Local)
│
├── 📁 hls_temp/ (Runtime)
│   └── *.m3u8, *.ts
│
├── 📄 package.json
├── 📄 tsconfig.json
│
└── 📚 Documentation/
    ├── README.md
    ├── DOCKER_IMPROVEMENTS.md
    ├── RECOMMENDATIONS.md
    ├── SUMMARY.md
    ├── TESTING_GUIDE.md
    └── ARCHITECTURE.md (ce fichier)
```

---

## 🔐 Sécurité

### Layers de Sécurité

```
┌─────────────────────────────────────────────────────────┐
│  Layer 1: Network Isolation                             │
│  • Réseau Docker dédié                                  │
│  • Pas d'accès direct entre conteneurs et host         │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 2: User Permissions                              │
│  • App tourne avec utilisateur non-root (nestjs)       │
│  • Permissions minimales                               │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 3: Secrets Management                            │
│  • .env non committé                                    │
│  • .env.example pour la doc                            │
│  • Variables d'environnement chiffrées                 │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 4: Vulnerability Scanning                        │
│  • Trivy scan automatique (CI/CD)                      │
│  • Scan des dépendances npm                            │
│  • Upload vers GitHub Security                         │
└─────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 5: Input Validation                              │
│  • class-validator sur tous les DTOs                   │
│  • Sanitization des chemins de fichiers                │
│  • Rate limiting (à implémenter)                       │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 Déploiement

### Environnement de Développement
```
Developer Machine
        │
        ▼
docker-compose up -d
        │
        ├─► PostgreSQL (localhost:5432)
        └─► NestJS App (localhost:3000)
```

### Environnement de Production (Futur)
```
┌─────────────────────────────────────────────────────────┐
│                    Load Balancer                        │
│                    (Nginx/Traefik)                      │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│  App Node 1 │ │  App Node 2 │ │  App Node 3 │
└──────┬──────┘ └──────┬──────┘ └──────┬──────┘
       │               │               │
       └───────────────┼───────────────┘
                       │
                       ▼
              ┌─────────────────┐
              │  PostgreSQL     │
              │  (Managed/RDS)  │
              └─────────────────┘
```

---

## 📈 Monitoring (Futur)

```
┌─────────────────────────────────────────────────────────┐
│                    Prometheus                           │
│                  (Metrics Collection)                   │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│  App        │ │  PostgreSQL │ │  Docker     │
│  Metrics    │ │  Metrics    │ │  Metrics    │
└─────────────┘ └─────────────┘ └─────────────┘
                     │
                     ▼
              ┌─────────────────┐
              │    Grafana      │
              │  (Dashboards)   │
              └─────────────────┘
```

---

## 🎯 Conclusion

Cette architecture offre :

✅ **Performance** : Build multi-stage, cache optimisé  
✅ **Sécurité** : Utilisateur non-root, isolation réseau  
✅ **Fiabilité** : Health checks, restart policies  
✅ **Scalabilité** : Prêt pour orchestration (K8s)  
✅ **Maintenabilité** : CI/CD, documentation complète  

**Votre application est prête pour la production ! 🚀**
